const CommandError = require('../CommandModules/command_error')
const BlacklistDeop = require('../util/blacklist util/blacklist_deop_selfcare.js')
const BlacklistMute = require('../util/blacklist util/blacklist_mute_selfcare.js')
module.exports = {
  name: 'blacklist',
hashOnly:true,
  execute (context) {
    const message = context.arguments.join(' ')
const bot = context.bot
    
  }
}
